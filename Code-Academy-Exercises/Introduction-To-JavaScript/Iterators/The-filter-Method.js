const randomNumbers = [375, 200, 3.14, 7, 13, 852];

// Call .filter() on randomNumbers below

const smallNumbers = randomNumbers.filter(small => small < 250);

console.log(smallNumbers);

const favoriteWords = ['nostalgia', 'hyperbole', 'fervent', 'esoteric', 'serene'];


// Call .filter() on favoriteWords below

const longFavortieWords = favoriteWords.filter(seven => {
  return seven.length > 7;})
console.log(longFavortieWords);





//  Used filter to create new array from existing array data
